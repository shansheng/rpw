<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.rpw.mapper.SubcontractorTeamMapper">

    <!-- 根据项目ID查询 -->
    <select id="selectByProjectId" resultType="com.company.rpw.entity.SubcontractorTeam">
        SELECT * FROM subcontractor_team
        WHERE project_id = #{projectId}
        AND deleted = 0
        ORDER BY id ASC
    </select>

    <!-- 根据专业工程模糊查询 -->
    <select id="selectByProfessionalEngineering" resultType="com.company.rpw.entity.SubcontractorTeam">
        SELECT * FROM subcontractor_team
        WHERE professional_engineering LIKE CONCAT('%', #{professionalEngineering}, '%')
        AND deleted = 0
        ORDER BY id ASC
    </select>

</mapper>
